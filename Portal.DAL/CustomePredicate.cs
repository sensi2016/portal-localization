using LinqKit;
using Portal.Entities.Models;
using System;
using System.Collections.Generic;
using System.Linq.Expressions;
using System.Text;

namespace Portal.DAL
{
    public class CustomePredicate
    {
        //public static Expression<Func<T,bool>> FullName<T>(Person person, string fullName)
        //{
        //    var predicate = PredicateBuilder.False<T>();

        //    predicate = predicate.And(d => d["FirstName"].Contains(fullName));

        //    return predicate;
        //}
    }
}
